name: firs Workflow

on:
  push:
    branches: [ "main" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Stazeni kodu z repozitare
      uses: actions/checkout@v4
    - name: Spusteni testovaciho skriptu
      run: echo "Workflow uspesne bezi!"
